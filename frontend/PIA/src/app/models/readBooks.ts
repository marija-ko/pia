export interface ReadBook {
    bookId: string, 
    userId: string, 
    genres: string[], 
    pagesRead: number, 
    pagesTotal: number,
    bookTitle: string,
    bookAuthors: string[]

    id: string
}